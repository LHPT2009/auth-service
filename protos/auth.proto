syntax = "proto3";

package auth;

service AuthService {
  rpc GetInfoRoleAndPermissions (UserIdRequest) returns (Auth) {}
}

message UserIdRequest {
  string id = 1;
}

message Auth {
  repeated string roles = 2;
  repeated string permissions = 3;
}